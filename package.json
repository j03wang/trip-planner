{
  "name": "structured-itinerary-map",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-browser-library.mjs",
    "build:check": "node scripts/build-browser-library.mjs --check",
    "check": "node scripts/check-project.mjs && npm run build:check",
    "test": "node --test",
    "validate:itinerary": "node scripts/validate-itinerary.mjs"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "happy-dom": "20.14.5",
    "maplibre-gl": "6.9.0"
  }
}
